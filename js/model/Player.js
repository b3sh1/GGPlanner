import {util} from "../utils.js";

class Player {
    constructor(id=-1) {
        this.id = id;
    }

    test(n) {
        return n+o.bla+util();
    }
}

const o = {
    'bla': 5,
}

export {Player};